pipeline {
    agent any

    environment {
        // URLs des services
        API_URL = 'http://localhost:5000'
        FRONTEND_URL = 'http://localhost:3000'
        TEST_DATABASE_URL = 'mongodb://localhost:27017/idurar_test'
        
        // Configuration des tests
        NODE_ENV = 'test'
        TEST_TIMEOUT = '30000'
    }

    stages {
        stage('Checkout') {
            steps {
                echo '🔍 Récupération du code...'
                checkout scm
            }
        }

        stage('Environment Setup') {
            steps {
                echo '🔧 Configuration de l\'environnement de test...'
                script {
                    // Vérifier que les services sont disponibles
                    bat '''
                        echo "Vérification des services..."
                        curl -f http://localhost:5000/health || echo "Backend non disponible"
                        curl -f http://localhost:3000 || echo "Frontend non disponible"
                    '''
                }
            }
        }

        stage('Install Dependencies') {
            steps {
                echo '📦 Installation des dépendances...'
                script {
                    bat '''
                        cd /d "%WORKSPACE%"
                        if exist backend\\package.json (
                            cd backend
                            npm install
                        )
                        if exist frontend\\package.json (
                            cd ..\\frontend
                            npm install
                        )
                        if exist tests\\package.json (
                            cd ..\\tests
                            npm install
                        )
                    '''
                }
            }
        }

        stage('Backend Tests') {
            steps {
                echo '🔥 Tests backend...'
                script {
                    bat '''
                        cd /d "%WORKSPACE%\\tests"
                        npm run test:baseline:backend
                    '''
                }
            }
        }

        stage('Frontend Tests') {
            steps {
                echo '🎨 Tests frontend...'
                script {
                    bat '''
                        cd /d "%WORKSPACE%\\tests"
                        npm run test:baseline:frontend
                    '''
                }
            }
        }

        stage('Performance Tests') {
            steps {
                echo '⚡ Tests de performance...'
                script {
                    bat '''
                        cd /d "%WORKSPACE%\\tests"
                        npm run test:baseline:performance
                    '''
                }
            }
        }

        stage('Security Tests') {
            steps {
                echo '🔒 Tests de sécurité...'
                script {
                    bat '''
                        cd /d "%WORKSPACE%\\tests"
                        npm audit
                    '''
                }
            }
        }

        stage('Resource Monitoring') {
            steps {
                echo '📊 Surveillance des ressources...'
                script {
                    bat '''
                        echo "=== RESSOURCES SYSTÈME ==="
                        docker stats --no-stream --format "table {{.Container}}\t{{.CPUPerc}}\t{{.MemUsage}}\t{{.NetIO}}\t{{.BlockIO}}"
                        echo "=== MÉTRIQUES DE TEST ==="
                        echo "Tests baseline optimisés terminés avec succès"
                    '''
                }
            }
        }
    }

    post {
        always {
            echo '🧹 Nettoyage...'
            script {
                bat '''
                    echo "Nettoyage des fichiers temporaires..."
                    if exist "%WORKSPACE%\\temp" rmdir /s /q "%WORKSPACE%\\temp"
                '''
            }
        }

        success {
            echo '✅ Tous les tests baseline ont réussi !'
            script {
                bat '''
                    echo "=== RÉSULTATS ==="
                    echo "✅ Tests backend: PASSED"
                    echo "✅ Tests frontend: PASSED"
                    echo "✅ Tests performance: PASSED"
                    echo "✅ Tests sécurité: PASSED"
                    echo "✅ Monitoring ressources: OK"
                '''
            }
        }

        failure {
            echo '❌ Certains tests ont échoué !'
            script {
                bat '''
                    echo "=== ÉCHECS ==="
                    echo "❌ Vérifiez les logs ci-dessus"
                    echo "❌ Vérifiez que les services sont démarrés"
                '''
            }
        }

        unstable {
            echo '⚠️ Tests terminés avec des avertissements !'
        }
    }
}

